@Library('my-shared-library') _

pipeline {
    agent any

    parameters {
        choice(name: 'ENVIRONMENT', choices: ['dev', 'test', 'prod'], description: 'Select the environment to deploy to.')
    }

    stages {
        stage('Notify Start') {
            steps {
                script {
                    // This is sending correctly for you
                    new com.helper.Notification(this).send("Started")
                }
            }
        }

        stage('Deployment Environment') {
            steps {
                echo "Deploying to ${params.ENVIRONMENT} environment..."
                // To test Success: Leave as is.
                // To test Failure: Uncomment next line -> error "Failure"
                // To test Unstable: Uncomment next line -> currentBuild.result = 'UNSTABLE'
            }
        }
    }

    // THE POST BLOCK MUST BE OUTSIDE OF STAGES
    post {
        success { 
            script { new com.helper.Notification(this).send("Success") } 
        }
        failure { 
            script { new com.helper.Notification(this).send("Failure") } 
        }
        unstable { 
            script { new com.helper.Notification(this).send("Unstable") } 
        }
    }
}
