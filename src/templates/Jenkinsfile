stage('Deploy') {
            steps {
                script {
                    echo "--- DEBUG: Testing Failure Notification ---"
                    // This command returns a non-zero exit code
                    // Jenkins sees this as a 'HARD FAILURE'
                    sh "exit 1" 
                }
            }
        }
